# REL Transformer 125M — Spider (OOM-safe: batch=8, acc=4, max_src=384)
model_type: relational
hidden_dim: 512
num_encoder_layers: 12
num_decoder_layers: 12
num_heads: 8
num_attributes: 8
ffn_dim: 2048
max_seq_len: 512
dropout: 0.2
vocab_size: 32000
learning_rate: 2.0e-4
weight_decay: 0.05
warmup_steps: 2000
max_steps: 100000
batch_size: 8
gradient_accumulation: 4
max_grad_norm: 1.0
label_smoothing: 0.15
eval_every: 1000
save_every: 10000
patience: 20
max_src_len: 384
max_tgt_len: 200
fp16: true
num_workers: 2
log_every: 100
